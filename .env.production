# For the exported web build (e.g. GitHub Pages) we enable demo mode so it works
# without needing to host the backend.
EXPO_PUBLIC_DEMO_MODE=true

# If you deploy to GitHub Pages as a project site, set this to your repo name:
# Example: /banana-bank
EXPO_PUBLIC_WEB_BASE_PATH=/banana-bank
